import { PageContainer } from "@/components/layout/page-container";

export default function CreatorVideosPage() {
  return (
    <PageContainer title="マイ動画一覧">
      <p className="text-gray-500">動画一覧が表示されます（実装予定）</p>
    </PageContainer>
  );
}
